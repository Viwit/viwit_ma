class PaymenClass {
  int balance;
  int user_id;
  int id;
  String token;

  int get getBalance => this.balance;
  set setBalance(int balance) => this.balance;

  int get getuser_id => this.user_id;
  set setuser_id(int user_id) => this.user_id;

  int get getid => this.id;
  set setid(int id) => this.id;

  String get gettoken => this.token;
  set settoken(String token) => this.token;
}
